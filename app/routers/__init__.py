"""API router modules for muFinances.

New endpoint groups should live here instead of adding more routes directly to
app.main. The legacy routes remain in main.py until their service areas are
lifted into routers in small, testable slices.
"""

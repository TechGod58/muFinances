{
  "name": "mufinances",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test:playwright": "playwright test",
    "test:playwright:ci": "playwright test tests/playwright --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
